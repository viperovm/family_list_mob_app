# Android SDK / JDK environment for this project.
# Source this file with: source .envrc (or copy into your shell profile).
export JAVA_HOME=/usr/local/opt/openjdk@17
export ANDROID_HOME=/Users/admin/Projects/lists_app/.android-tools/sdk
export ANDROID_SDK_ROOT="$ANDROID_HOME"
export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$ANDROID_HOME/emulator:$PATH"

# Backend API base URL.
# По умолчанию НЕ задаём — тогда используется боевой URL из
# src/shared/api/client.ts (https://listsapp.djangopirate.ru/api/v1).
# Для локальной разработки на Android-эмуляторе раскомментируйте строку ниже
# (host loopback = 10.0.2.2) и пересоберите приложение:
# export EXPO_PUBLIC_API_URL=http://10.0.2.2:8000/api/v1