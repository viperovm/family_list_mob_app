server {
    listen 80;
    server_name listsapp.djangopirate.ru _;

    client_max_body_size 20m;

    # Перенаправление всего трафика на HTTPS (раскомментируйте после настройки сертификатов).
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /app/media/;
        expires 30d;
    }

    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://web:8000;
        proxy_read_timeout 90s;
    }
}

# Блок HTTPS (раскомментируйте после получения сертификатов для домена).
# server {
#     listen 443 ssl http2;
#     server_name listsapp.djangopirate.ru;
#
#     ssl_certificate     /etc/nginx/certs/listsapp.djangopirate.ru.pem;
#     ssl_certificate_key /etc/nginx/certs/listsapp.djangopirate.ru.key;
#
#     location / {
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_pass http://web:8000;
#         proxy_read_timeout 90s;
#     }
# }