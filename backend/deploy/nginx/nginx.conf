# Хостовый nginx (edge-прокси) для listsapp.djangopirate.ru.
# Положите этот файл в /etc/nginx/sites-available/listsapp.djangopirate.ru
# и сделайте symlink в sites-enabled.
#
# Статика раздаётся напрямую из host-каталогов, куда Docker-контейнер "web"
# сохраняет её через bind-mount (./staticfiles и ./media в backend/).
#
# ВАЖНО: укажите ниже свой реальный абсолютный путь к каталогу backend/.
# Узнать его: cd ~/family_list_mob_app/backend && pwd

server {
    listen 80;
    server_name listsapp.djangopirate.ru;

    client_max_body_size 20m;

    location /static/ {
        # ЗАМЕНИТЕ путь на вывод команды `pwd` из backend/ + /staticfiles/
        alias /home/viperovm/family_list_mob_app/backend/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /home/viperovm/family_list_mob_app/backend/media/;
        expires 30d;
    }

    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://127.0.0.1:8000;
        proxy_read_timeout 90s;
    }
}

# Блок HTTPS (раскомментируйте после получения сертификатов для домена).
# server {
#     listen 443 ssl http2;
#     server_name listsapp.djangopirate.ru;
#
#     ssl_certificate     /etc/letsencrypt/live/listsapp.djangopirate.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/listsapp.djangopirate.ru/privkey.pem;
#
#     location /static/ {
#         alias /home/viperovm/family_list_mob_app/backend/staticfiles/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /media/ {
#         alias /home/viperovm/family_list_mob_app/backend/media/;
#         expires 30d;
#     }
#
#     location / {
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_pass http://127.0.0.1:8000;
#         proxy_read_timeout 90s;
#     }
# }